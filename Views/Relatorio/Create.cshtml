@model CasaRepousoWeb.Models.Relatorio

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Relatorio</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h2>Criar Novo Relatorio</h2>
        <form asp-action="Create" method="post">
            <input type="hidden" name="IdosoId" value="@ViewBag.IdosoId" />
            <div class="form-group">
                <label for="Titulo">Titulo </label>
                <input type="text" class="form-control" id="Titulo" name="Titulo" required />
            </div>
            <div class="form-group">
                <label for="Descricao">Descricao</label>
                <textarea class="form-control" placeholder="Descricao" name="Descricao"></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Salvar</button> <!-- Botão de Salvar -->
            <a href="/relatorio/read/@ViewBag.IdosoId" class="btn btn-secondary">Voltar</a>
        </form>
    </div>
</body>
</html>
